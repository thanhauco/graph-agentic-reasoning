export default function Chat() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Agent Chat</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        LangGraph agent + SSE streaming — wired in Phase 6.
      </p>
    </div>
  );
}
